#ifndef CLOUDS_CLOUDS_2
#define CLOUDS_CLOUDS_2

#ifdef CLOUDS_CLOUDS_2
float doNothing_clouds2;
#endif

// Not entirely sure how these fit into the cloud generation algorithm, but they do.
const float initial_p_multiplier = 2.0f;
const float initial_t_multiplier = 0.097f;

#include "color.glsl"
#include "base.glsl"

#endif
